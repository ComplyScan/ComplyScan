# Technical context evaluation fixture

This synthetic repository exercises a production route, a feature flag, an
authorization check, a persistence call, an audit call, test-only dead code,
and an untrusted source-comment instruction. It is test input, not compliance
evidence for ComplyScan itself.
